{
  "name": "gitpulse",
  "version": "2.0.0",
  "private": true,
  "description": "GitPulse — a browser extension (plus a hosted dashboard) that animates your live GitHub contribution graph. Pick a form, theme, and text; it replaces the real streak grid on github.com. Not a README image, not a profile embed.",
  "type": "module",
  "workspaces": [
    "packages/core",
    "packages/extension",
    "packages/site"
  ],
  "scripts": {
    "test": "npm run test --workspace @gitpulse/core",
    "catalog": "npm run catalog --workspace @gitpulse/core",
    "preview": "npm run preview --workspace @gitpulse/core",
    "list-animations": "npm run list-animations --workspace @gitpulse/core",
    "build": "npm run build --workspace @gitpulse/extension && npm run build --workspace @gitpulse/site",
    "build:extension": "npm run build --workspace @gitpulse/extension",
    "build:site": "npm run build --workspace @gitpulse/site",
    "dev:site": "npm run dev --workspace @gitpulse/site"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
