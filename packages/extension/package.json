{
  "name": "@gitpulse/extension",
  "version": "2.0.0",
  "description": "GitPulse browser extension (Chrome/Edge, Manifest V3): replaces the live GitHub contribution graph on github.com with an animated one.",
  "private": true,
  "type": "module",
  "scripts": {
    "icons": "node scripts/gen-icons.mjs",
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "clean": "rm -rf dist icons"
  },
  "dependencies": {
    "@gitpulse/core": "*"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
